package com.khaledamin.moviesapplication.domain.usecases

import com.khaledamin.moviesapplication.domain.repository.MoviesRepo
import javax.inject.Inject

//class GetMoviesUseCases @Inject constructor(
//    private val repo: MoviesRepo,
//) {
//    suspend fun invoke(page:Int, sortBy: String, fetchFromRemote:Boolean) = repo.getPopularMovies(page, sortBy, fetchFromRemote)
//}